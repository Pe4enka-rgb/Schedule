<Page
    x:Class="Schedule.View.EntityView.SchoolClassView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:evm="clr-namespace:Schedule.ViewModels.EntityViewModels"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance {x:Type evm:SchoolClassViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">


    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel Background="Snow">
        <WrapPanel Margin="5" DockPanel.Dock="Top">
            <Button
                Padding="3"
                Command="{Binding AddSchoolClassCommand}"
                Content="Добавить" />
            <Button
                Margin="3"
                Padding="3"
                Command="{Binding DeleteSchoolClassCommand}"
                CommandParameter="{Binding SelectedSchoolClass}"
                Content="Удалить" />
            <Button 
                Margin="3"
                Padding="3"
                Command="{Binding EditSchoolClassCommand}"
                CommandParameter="{Binding SelectedSchoolClass}"
                Content="Изменить" />
        </WrapPanel>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding ClassesModels}">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding SchoolClass.Grade}" Header="Год" />
                <DataGridTextColumn Binding="{Binding SchoolClass.Letter}" Header="Буква" />
                <DataGridTextColumn Binding="{Binding SchoolClass}" Header="Название" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Page>
